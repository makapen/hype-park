import { convertIndex } from '../../../helpers/convert-index';
import { module, test } from 'qunit';

module('Unit | Helper | convert index');

// Replace this with your real tests.
test('it works', function(assert) {
  var result = convertIndex(42);
  assert.ok(result);
});
