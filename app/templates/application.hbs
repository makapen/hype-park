{{#if controller.isIn}}

  {{partial 'topnav/default'}}

  {{outlet}}
{{else}}
  <form  {{action 'submitPassword'}}>
    <label>Enter password</label>
    {{input value=controller.password}}
    <button>Submit</button>
  </form>
{{/if}}
